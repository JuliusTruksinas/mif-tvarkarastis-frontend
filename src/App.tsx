import { ToastContainer } from 'react-toastify';
import Router from './config/Router/Router';

const App = () => {
  return (
    <>
      <Router />
      <ToastContainer />
    </>
  );
};

export default App;
